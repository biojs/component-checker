{
 "dir" : "/path/to/package", // for local testing. absolute path is better,
                             // relative should work too.
 "url" : "http://www.some.url/to/package" // if the package is online, use url.
                                          // github repos will resolve too
}
